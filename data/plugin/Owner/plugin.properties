class=net.psammead.linky.plugin.owner.OwnerPlugin
visible=user/channel,user/query
allowed=user/channel,user/query

# by default, Owner plugin commands are only visible and usable to owners
commands.allowed=owner/channel,owner/query
commands.visible=owner/channel,owner/query

# the login command overrides the default: it is always visible and usable
command.login.allowed=user/channel,user/query
command.login.visible=user/channel,user/query
