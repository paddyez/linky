# comma-separated list of personalities that should be loaded at startup
# personalities are independent of each other and each have a different nickname,
# join different channels and store their data in different directories
personalities=wikibot
